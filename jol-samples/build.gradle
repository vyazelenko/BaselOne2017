dependencies {
    compile "org.openjdk.jol:jol-core:${jolVersion}"
}

jar {
    manifest {
        attributes(
                'Main-Class': 'com.vyazelenko.baselone.jol.CollectionLayout'
        )
    }
}
